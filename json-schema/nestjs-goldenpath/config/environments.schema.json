{
  "$id": "https://raw.githubusercontent.com/ageras-com/schema-collection/v1/json-schema/nestjs-goldenpath/config/environments.schema.json",
  "title": "Environments configuration schema",
  "type": "object",
  "properties": {
    "environments": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "The name of the environment (e.g., development, staging, production).",
        "pattern": "^(?!default$)[a-zA-Z0-9_]+$"
      },
      "minItems": 1,
      "uniqueItems": true
    }
  }
}
