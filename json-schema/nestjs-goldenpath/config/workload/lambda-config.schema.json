{
  "$id": "https://raw.githubusercontent.com/ageras-com/schema-collection/v1/json-schema/golden-path/config/workload/lambda-config.schema.json#",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "arn": {
      "type": "string",
      "description": "The ARN of the Lambda function."
    },
    "region": {
      "type": "string",
      "description": "The AWS region where the Lambda function is deployed."
    },
    "name": {
      "type": "string",
      "description": "The name of the Lambda function."
    }
  }
}
