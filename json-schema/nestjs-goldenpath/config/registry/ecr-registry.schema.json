{
  "$id": "https://raw.githubusercontent.com/ageras-com/schema-collection/feat-config-add-migrations-db-schema/json-schema/nestjs-goldenpath/config/registry/ecr-registry.schema.json",
  "type": "object",
  "additionalProperties": false,
  "description": "The ECR container registry where the application's Docker image is stored.",
  "properties": {
    "type": {
      "type": "string",
      "enum": ["ecr"],
      "description": "The type of container registry."
    },
    "repository": {
      "type": "string",
      "description": "The name of the ECR repository."
    },
    "region": {
      "type": "string",
      "description": "The AWS region where the ECR repository is located."
    }
  },
  "required": ["type", "repository", "region"]
}
