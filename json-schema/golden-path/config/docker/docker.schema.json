{
  "$id": "https://raw.githubusercontent.com/ageras-com/schema-collection/v1/json-schema/golden-path/config/docker/docker.schema.json",
  "type": "object",
  "description": "Configuration for Docker build settings per environment.",
  "patternProperties": {
    "^(default)[_a-zA-Z][a-zA-Z0-9_-]*$": {
      "$ref": "#/definitions/docker-environment-config",
      "description": "The environment ECS configuration that overrides the default configuration."
    }
  },
  "minProperties": 1,
  "definitions": {
    "docker-environment-config": {
      "type": "object",
      "description": "Docker build configuration for a specific environment.",
      "properties": {
        "context": {
          "type": "string",
          "description": "Specifies the working directory for the Docker build command."
        },
        "file": {
          "type": "string",
          "description": "Specifies the name of the Dockerfile to use for the build."
        },
        "target": {
          "type": "string",
          "description": "Sets the target build stage to build in a multi-stage Dockerfile."
        }
      },
      "additionalProperties": false
    }
  }
}