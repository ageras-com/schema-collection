{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "https://raw.githubusercontent.com/ageras-com/schema-collection/v1/json-schema/datadog-catalog/v3/partials/extensions-defaults.schema.json",
  "title": "Shine default extensions fields",
  "description": "Shared extensions constraints applied to every Datadog catalog entity type: area and domain. These live in extensions rather than spec because the upstream schema has additionalProperties: false on spec.",
  "type": "object",
  "properties": {
    "area": {
      "description": "Organisational area that owns this entity.",
      "type": "string"
    },
    "domain": {
      "description": "Business domain this entity belongs to.",
      "type": "string"
    }
  }
}
