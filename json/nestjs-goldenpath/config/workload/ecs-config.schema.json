{
  "$id": "https://raw.githubusercontent.com/ageras-com/schema-collection/feat-add-goldenpath-config/json/nestjs-goldenpath/config/workload/ecs-config.schema.json#",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "arn": {
      "type": "string",
      "description": "The ARN of the ECS service."
    },
    "region": {
      "type": "string",
      "description": "The AWS region where the ECS service is deployed."
    },
    "cluster": {
      "type": "string",
      "description": "The name of the ECS cluster."
    },
    "service": {
      "type": "string",
      "description": "The name of the ECS service."
    },
    "task-definition": {
      "type": "string",
      "description": "The definition of the ECS task."
    },
    "container-name": {
      "type": "string",
      "description": "The name of the container within the ECS task."
    }
  }
}
